services:
  db:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: lunov
      POSTGRES_PASSWORD: lunov
      POSTGRES_DB: lunov
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data: